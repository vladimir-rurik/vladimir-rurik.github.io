/**
 * Functions written here will be useful in subsequent lessons
 * Using these functions we will add elements to the list to check dynamic loading
 * Therefore, ideally, the functions should return random data, but at the same time not gibberish.
 * In general, do it as it will be convenient for you.
 * */

/**
 * You need to create a Category type, it will be used below.
 * Category contains
 * - id (string)
 * - name (string)
 * - photo (string, optional)
 *
 * Product contains
 * - id (string)
 * - name (string)
 * - photo (string)
 * - desc (string, optional)
 * - createdAt (string)
 * - oldPrice (number, optional)
 * - price (number)
 * - category (Category)
 *
 * Operation can be either a Cost or a Profit
 *
 * Cost contains
 * - id (string)
 * - name (string)
 * - desc (string, optional)
 * - createdAt (string)
 * - amount (number)
 * - category (Category)
 * - type ('Cost')
 *
 * Profit contains
 * - id (string)
 * - name (string)
 * - desc (string, optional)
 * - createdAt (string)
 * - amount (number)
 * - category (Category)
 * - type ('Profit')
 * */

/**
 * Creates a random product (Product).
 * Takes a creation date (string)
 * */
// export const createRandomProduct = (createdAt: string) => {};

/**
 * Creates a random operation (Operation).
 * Takes a creation date (string)
 * */
// export const createRandomOperation = (createdAt: string) => {};
